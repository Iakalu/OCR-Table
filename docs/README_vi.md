# Docs (VI)

Bo tai lieu trong `docs/` duoc gom lai de de doc va khop voi code/config hien tai.

- `running_and_tuning_vi.md`: cai dat, OCR backend, cach chay web/CLI, va tinh chinh tham so.
- `design_vi.md`: mo ta pipeline va thiet ke full pipeline (Table Transformer + OCR).
- `training_vi.md`: huong dan train `LineSegNet` (structure line CNN) va train tu remote manifest / streaming dataset.

